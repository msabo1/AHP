import { AlternativeRequest } from './alternative-request';

describe('AlternativeRequest', () => {
  it('should create an instance', () => {
    expect(new AlternativeRequest()).toBeTruthy();
  });
});
