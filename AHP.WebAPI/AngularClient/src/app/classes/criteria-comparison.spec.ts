import { CriteriaComparison } from './criteria-comparison';

describe('CriteriaComparison', () => {
  it('should create an instance', () => {
    expect(new CriteriaComparison()).toBeTruthy();
  });
});
